@using Microsoft.Extensions.Localization
@model IEnumerable<CoreServiceCollection.Localisation.Models.PersonViewModel>
@inject IStringLocalizer<SharedResources> SharedLocalizer

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">@SharedLocalizer["Nouveau"]</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AdditionalInfo)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdditionalInfo)
            </td>
            <td>
                @Html.ActionLink(SharedLocalizer["Édition"], "Edit", new {id = item.Id}) |
                @Html.ActionLink(SharedLocalizer["Détails"], "Details", new {id = item.Id}) |
                @Html.ActionLink(SharedLocalizer["Supprimer"], "Delete", new {id = item.Id})
            </td>
        </tr>
    }
    </tbody>
</table>
